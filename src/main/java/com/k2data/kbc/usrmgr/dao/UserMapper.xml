<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.k2data.kbc.usrmgr.dao.UserMapper">
    <resultMap id="BaseResultMap" type="com.k2data.kbc.usrmgr.model.User">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="password" jdbcType="VARCHAR" property="password"/>
    </resultMap>

    <insert id="insert" parameterType="com.k2data.kbc.usrmgr.model.User" useGeneratedKeys="true" keyProperty="id">
      INSERT INTO user (
        id,
        name,
        email,
        password
      )
      VALUES (
        #{id},
        #{name},
        #{email},
        #{password}
      )
    </insert>

    <delete id="delete">
        DELETE
        FROM user
        WHERE id = #{arg0}
    </delete>

    <update id="update" parameterType="com.k2data.kbc.usrmgr.model.User">
        UPDATE user
        <set>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="email != null">
                email = #{email,jdbcType=INTEGER},
            </if>
            <if test="password != null">
                password = #{password,jdbcType=INTEGER},
            </if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

    <select id="list"
        parameterType="com.k2data.kbc.usrmgr.dao.condition.UserCondition"
        resultMap="BaseResultMap">
        SELECT *
        FROM user
        WHERE 1 = 1
        <if test="fuzzyName != null and fuzzyName != ''">
            <![CDATA[ AND name LIKE CONCAT('%', #{fuzzyName},'%')]]>
        </if>
        <if test="ids != null and ids.size > 0">
            <![CDATA[ AND id IN ]]>
            <foreach close=")" collection="ids" index="index" item="item" open="("
                separator=",">
                <![CDATA[ #{item} ]]>
            </foreach>
        </if>
    </select>

    <select id="getById" resultMap="BaseResultMap">
        SELECT *
        FROM user
        WHERE id = #{arg0}
    </select>
</mapper>